"use strict";
{
    // data type ar two type :
    // 1. primitive (number ,string ,boolean , null, undefind, symbol)
    //string
    let firstName = "rezwan hossen ";
    // number
    let rollnumber = 20221033010;
    // boolean
    let isAdmin = true;
    // undefind
    let x = undefined;
    // null
    let y = null;
    // 2. nonprimitive( array , tuple , Object)
    //array
    let friends = ["rabbu", "galib", "jubber"];
    let age = [24, 23, 25];
    //tuple is a type of array . tuple-->array-->order-->type of value
    let coordenates = [1, 5];
    let ageName = [30, "abdullah"];
    // ageName[0]=23 but ageName[0]='23' not accpeted
    //object , optional and literal types
    const user = {
        company: "programming hero",
        fastName: "rezwan",
        middleName: "",
        lastName: "hossen",
    };
}
